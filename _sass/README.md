# _sass/

`main.scss` — all site styling in a single file (colour variables, layout,
component styles, dark mode, responsive breakpoints). Compiled into the
site's CSS via `assets/css/main.scss`, which just does `@import "main"`.

You won't need to touch this for routine content updates (adding a student,
a publication, etc.) — only when changing visual design.
