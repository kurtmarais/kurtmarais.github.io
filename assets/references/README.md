# assets/references/

`marais-2026.bib` — BibTeX bibliography. Not used by the site build; kept
as a manual reference file. It is still publicly downloadable at
`/assets/references/marais-2026.bib`.

The Publications page is built from the `_publications/` collection (see
`_publications/README.md`), so changes here don't appear on the site.
