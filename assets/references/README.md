# assets/references/

`marais-2026.bib` — a BibTeX file. Currently not referenced or used anywhere
in the site build (no template pulls from it) — it looks like it's kept here
purely as a personal/manual bibliography backup, separate from the
`_publications/` collection, which is what actually populates the
Publications page (see `_publications/README.md`). If you want your
publications' BibTeX entries to show on the site itself, that would need new
template work — right now this file is just storage.
