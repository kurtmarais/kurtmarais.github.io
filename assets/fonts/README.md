# assets/fonts/

Self-hosted Gotham webfont files (`.woff2`, three weights: book, medium,
bold), referenced via `@font-face` in `_sass/main.scss`. Only touch this if
you're changing the site's typeface.
