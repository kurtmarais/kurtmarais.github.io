# assets/libs/

Third-party front-end libraries, vendored directly (not via a package
manager): Bootstrap and Font Awesome. Don't hand-edit files inside `bootstrap/`
or `fontawesome/` — if you need to update a library version, replace the
whole subfolder with the new release.
